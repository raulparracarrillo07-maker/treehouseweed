// TreeHouseWeed — carrito de compras
// Guarda el pedido del cliente y arma el mensaje de WhatsApp para cerrar la venta.

const carrito = [];

function agregarAlCarrito(producto) {
  carrito.push(producto);
}
